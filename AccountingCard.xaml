<Window x:Class="TransportDepartment.AccountingCard"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:TransportDepartment"
        Loaded="AccountingCard_Loaded"
        WindowState="Maximized"
        WindowStartupLocation="CenterScreen"
        mc:Ignorable="d"
        Height="450" Width="800">

    <Window.Resources>
        <!-- Палитра цветов -->
        <SolidColorBrush x:Key="PrimaryColor" Color="#2C3E50"/>
        <SolidColorBrush x:Key="AccentColor" Color="#3498DB"/>
        <SolidColorBrush x:Key="AccentCloseColor" Color="#FFFF0000"/>

        <SolidColorBrush x:Key="TextColor" Color="#333333"/>
        <SolidColorBrush x:Key="HoverColor" Color="#2980B9"/>
        <SolidColorBrush x:Key="PressedColor" Color="#1C5980"/> 

        <!--Cтиль для текста-->
        <Style x:Key="ModernTextStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value ="20"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Foreground" Value="#2C3E50"/>
            <Setter Property="TextAlignment" Value="Center"/>
            <Setter Property="Margin" Value="0,0,0,0"/>
        </Style>
            <!-- Стиль для кнопок -->
        <Style x:Key="ModernButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="{StaticResource AccentColor}"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Padding" Value="20,12"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                                CornerRadius="8" 
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="{StaticResource HoverColor}"/>
                </Trigger>
                <Trigger Property="IsPressed" Value="True">
                    <Setter Property="Background" Value="{StaticResource PressedColor}"/>
                </Trigger>
            </Style.Triggers>
        </Style>
        <Style x:Key="ModernButtonCloseStyle" TargetType="Button">
            <Setter Property="Background" Value="{StaticResource AccentCloseColor}"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Padding" Value="20,12"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                                CornerRadius="8" 
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>

        </Style>

    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="30*"/>
            <RowDefinition Height="300*"/>
            <RowDefinition Height="20*"/>

        </Grid.RowDefinitions>
        <TextBlock TextWrapping="Wrap" Style="{StaticResource ModernTextStyle}">
    <Run>Карточка учета эксплуатации автомобиля</Run>
    <LineBreak/>
    <Run>Государственный номер: </Run>
    <Run FontWeight="Bold" Text="{Binding StateNumber}"/>
    <Run> Марка:</Run>
    <Run FontWeight="Bold" Text="{Binding TransportBrand}"/>
    <LineBreak/>
    <Run>период: </Run>
                <Run FontWeight="Bold" Text="{Binding Month}"/>

    <Run>Вставить год</Run>
                            <Run FontWeight="Bold" Text="{Binding Year}"/>

        </TextBlock>
        <DataGrid Grid.Row="1" />
        <Button Grid.Row="2" 
                x:Name="onMainWindow"
                Content="ВЕРНУТЬСЯ НА ГЛАВНЫЙ ЭКРАН"
                Style="{StaticResource ModernButtonStyle}"
                HorizontalAlignment="Left"
                VerticalAlignment="Center"
                Margin="10,0,0,0" Click="onMainWindow_Click" Height="45" Width="289"
                />
    </Grid>
</Window>
